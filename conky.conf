conky.config = {
  alignment = 'bottom_right',
  background = true,
  border_width = 1,
  cpu_avg_samples = 2,
  default_color = 'white',
  default_outline_color = 'white',
  default_shade_color = 'white',
  double_buffer = true,
  draw_borders = true,
  draw_graph_borders = true,
  draw_outline = false,
  draw_shades = false,
  extra_newline = false,
  font = 'DejaVu Sans Mono:size=10',
  gap_x = 5,
  gap_y = 30,
  minimum_width = 5,
  net_avg_samples = 2,
  no_buffers = true,
  out_to_console = false,
  out_to_stderr = false,
  own_window = false,
  own_window_class = 'Conky',
  own_window_transparent = true,
  own_window_type = 'desktop',
  short_units = false,
  show_graph_range = false,
  show_graph_scale = false,
  stippled_borders = 0,
  update_interval = 1.0,
  uppercase = false,
  use_spacer = 'left',
  use_xft = true,
  minimum_height = 5,

};

conky.text = [[
${color grey}
Arch Linux ($kernel) on Bermudes
$hr
${color grey}RAM Usage:  $memperc% ${color blue} ${membar 10}
${color grey}CPU1 Usage:$color ${cpu cpu1}% ${color red} ${cpubar cpu1 10}
${color grey}CPU2 Usage:$color ${cpu cpu2}% ${color red} ${cpubar cpu2 10}
${color grey}CPU3 Usage:$color ${cpu cpu3}% ${color red} ${cpubar cpu3 10}
${color grey}CPU4 Usage:$color ${cpu cpu4}% ${color red} ${cpubar cpu4 10}
${color grey}$hr
${color grey}ACPI temp:${color yellow} $acpitemp° C
${color grey}Batt: ${color yellow}$battery ${color red}${battery_bar 10}
     ${color yellow} $battery_time
${color grey}$hr
${color grey}Disk used: ${fs_used_perc /}% ${color blue} ${fs_bar 10 /}
${color grey}$hr
Playing: ${color green} ${scroll 15 $mpd_artist - $mpd_title} ${mpd_elapsed} / ${mpd_length}
]];

-- vim: set filetype=lua:
